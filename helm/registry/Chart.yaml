apiVersion: v2
type: application

name: registry
description: Local Docker registry, available at registry.local:5000.

# Chart version, expected to follow Semantic Versioning <https://semver.org/>
version: 0.1.0
