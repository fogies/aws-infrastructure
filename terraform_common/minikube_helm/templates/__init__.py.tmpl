from invoke import Collection

from .tasks import *

# Collection configured for expected use
ns = Collection()

ns.add_task(ssh)
ns.add_task(ssh_port_forward)
ns.add_task(install_chart)

ns.configure({
    '${instance_name}': {
        'instance_dir': '${instance_dir}',
        'instance_ip': '${instance_ip}',
        'instance_identity_file': '${instance_identity_file}'
    }
})

del Collection
